package com.group12.repository;

import com.group12.model.RollCallAndTimekeeping;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RollCallAndTimekeepingRepository extends JpaRepository<RollCallAndTimekeeping, Long> {
}
